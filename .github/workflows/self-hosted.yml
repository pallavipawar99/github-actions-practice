name: Self Hosted Label Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: [self-hosted, my-linux-runner]

    steps:
      - name: Check runner
        run: |
          echo "Running on labeled runner"
          hostname
          whoami