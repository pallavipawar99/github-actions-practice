name: Self Hosted Test

on: push

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Check runner
        run: |
          echo "Running on self hosted runner"
          hostname
          whoami
