name: Frist workflow

on:
    push:

jobs:
    run_first_workflow:
        runs-on: ubuntu-latest
        steps:
            - name: test job run
              run: |
                echo job run success
